#include "scene_layer.hpp"

namespace narechi::player
{
    scene_layer::scene_layer()
        : layer("SceneLayer")
    {
    }

    void scene_layer::on_attach()
    {
        // Attempt to find scene/project and load it
    }
}
